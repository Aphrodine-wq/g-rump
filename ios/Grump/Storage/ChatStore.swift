import Foundation
import SwiftData

class ChatStore {
    static func configure(modelContext: ModelContext) {
        // SwiftData configuration is handled by the ModelContainer
        // This class can be extended for additional storage operations
    }
}

